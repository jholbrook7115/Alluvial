---------------------------Issues/Things to Fix---------------------------
- Fix colors
- restrict accepted input of textfeild
- doublecheck scaling and scaling-origins
- set min/max widths

---------------------------Additions---------------------------
- song length display
- change main display bg color to compliment the album artwork
- create more features for the Settings Menu

---------------------------New Views/Windows---------------------------



---------------------------Need to Add Functionality to---------------------------------
- Settings to save: (referring to svae data beyond the lifecycle of the program  i.e. after you close the program)
    Playback Bar
        > volume
        > metadata(song length) from last song played(maybe)
        > shuffle state
        > loop state
    Playlist Panel
        > last selected selected playlist
    Main
        > metadata from last song played
            -artist
            -Album
            -songname
            -(other info)?

        NOTE: Should we save all relevent metadata?
                or
              Just a hash/reference to the song and let the client request the metadata from the server on startup?

    Settings Window
        > Usernames and passwords (for Spotify, Soundcloud, etc...)
        > local file path
        > Playback options
        > Server Options
            - IP/ other relevent network information
        > Alluvial User Settings (client login info for Alluvial Program)
        > (other info?)

newtask

